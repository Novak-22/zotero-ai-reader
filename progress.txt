# Zotero AI Reader Plugin - Progress

## Completed

- [x] 项目文档初始化
  - PRD.md, APP_FLOW.md, TECH_STACK.md, FRONTEND_GUIDELINES.md, BACKEND_STRUCTURE.md, IMPLEMENTATION_PLAN.md, CLAUDE.md, lessons.md

- [x] Phase 1: 基础框架
  - [x] 插件骨架搭建 (addon ID: ai-reader@novak22.com)
  - [x] LLM服务层基础 (LLMService + 3 providers: OpenAI, Ollama, Generic)
  - [x] PDF服务基础 (PDFService with text extraction + paragraph parsing)
  - [x] 聊天服务基础 (ChatService + ChatStorage with SQLite)
  - [x] UI框架 (ItemPaneManager sections: AI TOC + AI Chat)
  - [x] TypeScript类型修复 (DOM globals, TOCItem接口)

- [x] Phase 2: 插件成功加载
  - [x] 修复 src/index.ts: Zotero[addonInstance] 赋值实例而非类
  - [x] 修复 bootstrap.js: rootURI 已有尾部斜杠，去掉多余的 /
  - [x] 修复 ChatStorage: 构造函数不调用 Zotero API，改为懒加载
  - [x] 修复 registerSection: 补充必填的 onRender 回调
  - [x] 验证: Zotero.AddonAIReader.data.initialized === true
  - [x] 验证: AI TOC 和 AI Chat 两个 section 在 item pane 中可见

## Phase 3: AI TOC 功能优化 (2026-05-08)

### 已完成
- [x] **PDF页面导航修复**
  - 发现 Zotero.Reader._readers[0]._internalReader._primaryView 包含 PDFView
  - PDFView.navigate({ pageIndex }) 方法可用于页面跳转
  - 更新 navigateToPDFPage() 使用正确的方法

- [x] **TOC UI 美化**
  - 添加 .ai-toc-title 样式：文字溢出省略号
  - 添加 .ai-toc-page 样式：页面标签显示为圆角标签
  - 添加 .ai-toc-loading 样式：加载动画
  - 优化图标大小和层级间距

### 技术细节
- **页面导航发现过程**：
  1. Zotero.Reader.getByTabID(tabID) 获取 ReaderTab 实例
  2. ReaderTab._internalReader 包含实际阅读器
  3. _internalReader._primaryView 是 pdf_view_PDFView 类型
  4. PDFView.navigate({ pageIndex }) 可导航到指定页面（0-indexed）

### Git 提交
- `0acef7c` fix: Use Zotero.Reader._readers[0]._internalReader._primaryView.navigate for page navigation
- `02b5c70` enhance: Add TOC item styling - icons, ellipsis, page badges, and loading spinner

## 当前状态

AI TOC 功能：
- ✅ 插件成功加载并显示 AI TOC 和 AI Chat 两个面板
- ✅ 点击刷新按钮可生成目录（调用 LLM）
- ✅ 目录以层级形式显示（level-1/2/3）
- ✅ 点击目录项可导航到 PDF 对应页面
- ✅ UI 美化：加载动画、标题省略、页码标签

## 导航实现说明 (2026-05-08)

### 发现过程
1. Zotero.Reader._readers 包含所有打开的阅读器
2. ReaderTab._internalReader 包含实际阅读器实现
3. _internalReader._primaryView 是 pdf_view_PDFView 类型
4. PDFView.navigate({ pageIndex }) 方法接受 0-indexed 页码

### 实现代码
```typescript
async function navigateToPDFPage(page: number): Promise<void> {
  const readers = Zotero.Reader._readers;
  if (!readers || readers.length === 0) return;

  const primaryView = readers[0]._internalReader?._primaryView;
  if (!primaryView) return;

  if (typeof primaryView.navigate === "function") {
    await primaryView.navigate({ pageIndex: page - 1 });
  }
}
```

### 注意事项
- navigate() 返回 Promise，需要 await
- 页码参数是 0-indexed（所以 page 1 传入 pageIndex: 0）
- MCP 执行上下文可能无法准确读取导航后的 pageIndex（状态同步问题）

## 待验证
- [ ] TOC 点击导航是否正确工作（需在 Zotero 中测试）
- [ ] 多段落 PDF 的 paragraphIndex 映射是否正确
- [ ] AI Chat 功能接入

## Next Steps

1. **AI TOC 验证测试**
   - 在 Zotero 中打开 PDF，点击 TOC 刷新按钮
   - 验证目录生成成功
   - 点击目录项，验证 PDF 跳转

2. **AI Chat 功能接通**
   - 配置 LLM API Key（设置界面）
   - 发送消息 → 调用 LLMService → 显示回复
   - 支持选中 PDF 文本作为上下文

3. **设置界面**
   - LLM provider 选择 (OpenAI / Ollama / Generic)
   - API Key 输入
   - Model 名称配置

---

Last Updated: 2026-05-08